getSeconds
// dùng để lấy số giây của một thời điểm cụ thể 
getMinutes
//dùng để lấy số phút của thời điểm cụ thể
getHours
//  dùng để lấy số giờ của một thời điểm cụ thể (từ 0 đến 23, theo định dạng 24 giờ).
 
new Date() 
//  trong JavaScript được sử dụng để tạo một đối tượng Date, giúp làm việc với thời gian và ngày tháng.
// (lấy giờ hiện tại)


setInterval(function, milliseconds);
function: Hàm sẽ được thực thi lặp lại.

milliseconds: Khoảng thời gian giữa các lần thực thi (tính bằng mili-giây, 1000ms = 1 giây).


//css 
rotate deg
được sử dụng để xoay phần tử một góc nhất định theo chiều kim đồng hồ 
hoặc ngược chiều kim đồng hồ. Giá trị góc được chỉ định bằng độ (deg).

